# Техническое задание: Оптимизация производительности базы данных

## Общая информация
- **Приоритет:** normal
- **Оценка:** 2 SP
- **Исполнитель:** backend
- **Срок выполнения:** 2 дня

## Цель
Провести анализ и оптимизацию запросов к базе данных для повышения скорости работы приложения, уменьшения времени отклика и улучшения общей производительности системы.

## Требования

### Функциональные требования
1. **Query Optimization:** Оптимизация всех медленных запросов
2. **Indexing Strategy:** Создание оптимальных индексов
3. **Connection Pooling:** Настройка пула соединений
4. **Caching Layer:** Внедрение кэширования
5. **Monitoring:** Мониторинг производительности БД

### Нефункциональные требования
1. **Response Time:** Время отклика < 100ms для простых запросов
2. **Throughput:** Поддержка 1000+ запросов в секунду
3. **Scalability:** Горизонтальное масштабирование
4. **Reliability:** 99.9% uptime
5. **Resource Usage:** Оптимальное использование ресурсов

## Анализ текущей производительности

### 1. Performance Baseline
```sql
-- Анализ медленных запросов
SELECT 
    query,
    mean_time,
    calls,
    total_time,
    rows
FROM pg_stat_statements 
WHERE mean_time > 100 -- запросы медленнее 100ms
ORDER BY mean_time DESC;

-- Анализ использования индексов
SELECT 
    schemaname,
    tablename,
    indexname,
    idx_scan,
    idx_tup_read,
    idx_tup_fetch
FROM pg_stat_user_indexes
ORDER BY idx_scan DESC;

-- Анализ размеров таблиц
SELECT 
    schemaname,
    tablename,
    pg_size_pretty(pg_total_relation_size(schemaname||'.'||tablename)) as size
FROM pg_tables 
WHERE schemaname = 'public'
ORDER BY pg_total_relation_size(schemaname||'.'||tablename) DESC;
```

### 2. Query Analysis
```javascript
// MongoDB query analysis
db.users.find({ email: "user@example.com" }).explain("executionStats");

// PostgreSQL query analysis
EXPLAIN (ANALYZE, BUFFERS) 
SELECT * FROM users WHERE email = 'user@example.com';

// Slow query log analysis
const slowQueries = await db.collection('slow_queries').find({
  executionTime: { $gt: 100 } // queries slower than 100ms
}).sort({ executionTime: -1 });
```

## Стратегия оптимизации

### 1. Indexing Strategy

#### Primary Indexes
```sql
-- User table indexes
CREATE INDEX CONCURRENTLY idx_users_email ON users(email);
CREATE INDEX CONCURRENTLY idx_users_created_at ON users(created_at);
CREATE INDEX CONCURRENTLY idx_users_active ON users(is_active) WHERE is_active = true;

-- Payment table indexes
CREATE INDEX CONCURRENTLY idx_payments_user_id ON payments(user_id);
CREATE INDEX CONCURRENTLY idx_payments_status ON payments(status);
CREATE INDEX CONCURRENTLY idx_payments_created_at ON payments(created_at);
CREATE INDEX CONCURRENTLY idx_payments_amount ON payments(amount);
CREATE INDEX CONCURRENTLY idx_payments_user_status ON payments(user_id, status);

-- Composite indexes for common queries
CREATE INDEX CONCURRENTLY idx_payments_user_created ON payments(user_id, created_at DESC);
CREATE INDEX CONCURRENTLY idx_payments_status_created ON payments(status, created_at DESC);
```

#### Partial Indexes
```sql
-- Index for active users only
CREATE INDEX CONCURRENTLY idx_users_active_email 
ON users(email) WHERE is_active = true;

-- Index for completed payments only
CREATE INDEX CONCURRENTLY idx_payments_completed 
ON payments(user_id, created_at DESC) WHERE status = 'completed';

-- Index for recent payments
CREATE INDEX CONCURRENTLY idx_payments_recent 
ON payments(user_id, created_at DESC) 
WHERE created_at > NOW() - INTERVAL '30 days';
```

#### Covering Indexes
```sql
-- Covering index for user profile queries
CREATE INDEX CONCURRENTLY idx_users_profile 
ON users(email, first_name, last_name, created_at) 
INCLUDE (id, is_active);

-- Covering index for payment history
CREATE INDEX CONCURRENTLY idx_payments_history 
ON payments(user_id, created_at DESC) 
INCLUDE (amount, currency, status, description);
```

### 2. Query Optimization

#### Optimized User Queries
```javascript
// Before optimization
const getUserByEmail = async (email) => {
  return await User.findOne({ email });
};

// After optimization with projection
const getUserByEmail = async (email) => {
  return await User.findOne(
    { email },
    { 
      password: 0, // exclude password field
      __v: 0 
    }
  );
};

// Optimized user search with pagination
const searchUsers = async (query, page = 1, limit = 10) => {
  const skip = (page - 1) * limit;
  
  return await User.find({
    $or: [
      { firstName: { $regex: query, $options: 'i' } },
      { lastName: { $regex: query, $options: 'i' } },
      { email: { $regex: query, $options: 'i' } }
    ],
    isActive: true
  })
  .select('firstName lastName email createdAt')
  .sort({ createdAt: -1 })
  .skip(skip)
  .limit(limit)
  .lean(); // use lean() for better performance
};
```

#### Optimized Payment Queries
```javascript
// Optimized payment history with aggregation
const getPaymentHistory = async (userId, filters = {}) => {
  const pipeline = [
    { $match: { userId: new ObjectId(userId) } },
    { $sort: { createdAt: -1 } }
  ];
  
  if (filters.status) {
    pipeline[0].$match.status = filters.status;
  }
  
  if (filters.dateFrom || filters.dateTo) {
    pipeline[0].$match.createdAt = {};
    if (filters.dateFrom) {
      pipeline[0].$match.createdAt.$gte = new Date(filters.dateFrom);
    }
    if (filters.dateTo) {
      pipeline[0].$match.createdAt.$lte = new Date(filters.dateTo);
    }
  }
  
  // Add pagination
  if (filters.page && filters.limit) {
    pipeline.push(
      { $skip: (filters.page - 1) * filters.limit },
      { $limit: filters.limit }
    );
  }
  
  // Add aggregation for statistics
  pipeline.push({
    $group: {
      _id: null,
      totalAmount: { $sum: '$amount' },
      averageAmount: { $avg: '$amount' },
      count: { $sum: 1 }
    }
  });
  
  return await Payment.aggregate(pipeline);
};
```

### 3. Connection Pooling

#### PostgreSQL Connection Pool
```javascript
const { Pool } = require('pg');

const pool = new Pool({
  user: process.env.DB_USER,
  host: process.env.DB_HOST,
  database: process.env.DB_NAME,
  password: process.env.DB_PASSWORD,
  port: process.env.DB_PORT,
  max: 20, // maximum number of clients in the pool
  min: 5,  // minimum number of clients in the pool
  idleTimeoutMillis: 30000, // close idle clients after 30 seconds
  connectionTimeoutMillis: 2000, // return an error after 2 seconds if connection could not be established
  maxUses: 7500, // close (and replace) a connection after it has been used 7500 times
});

// Connection pool monitoring
pool.on('connect', (client) => {
  console.log('New client connected to database');
});

pool.on('error', (err, client) => {
  console.error('Unexpected error on idle client', err);
  process.exit(-1);
});

module.exports = pool;
```

#### MongoDB Connection Pool
```javascript
const mongoose = require('mongoose');

const connectDB = async () => {
  try {
    const conn = await mongoose.connect(process.env.MONGODB_URI, {
      maxPoolSize: 10, // Maintain up to 10 socket connections
      serverSelectionTimeoutMS: 5000, // Keep trying to send operations for 5 seconds
      socketTimeoutMS: 45000, // Close sockets after 45 seconds of inactivity
      bufferMaxEntries: 0, // Disable mongoose buffering
      bufferCommands: false, // Disable mongoose buffering
    });
    
    console.log(`MongoDB Connected: ${conn.connection.host}`);
  } catch (error) {
    console.error('Database connection error:', error);
    process.exit(1);
  }
};

module.exports = connectDB;
```

### 4. Caching Strategy

#### Redis Caching Layer
```javascript
const redis = require('redis');
const client = redis.createClient({
  host: process.env.REDIS_HOST,
  port: process.env.REDIS_PORT,
  password: process.env.REDIS_PASSWORD,
  retry_strategy: (options) => {
    if (options.error && options.error.code === 'ECONNREFUSED') {
      return new Error('The server refused the connection');
    }
    if (options.total_retry_time > 1000 * 60 * 60) {
      return new Error('Retry time exhausted');
    }
    if (options.attempt > 10) {
      return undefined;
    }
    return Math.min(options.attempt * 100, 3000);
  }
});

// Cache middleware
const cache = (duration) => {
  return async (req, res, next) => {
    const key = `cache:${req.originalUrl}`;
    
    try {
      const cached = await client.get(key);
      if (cached) {
        return res.json(JSON.parse(cached));
      }
      
      // Store original json method
      const originalJson = res.json;
      
      // Override json method to cache response
      res.json = function(body) {
        // Cache the response
        client.setex(key, duration, JSON.stringify(body));
        
        // Call original json method
        originalJson.call(this, body);
      };
      
      next();
    } catch (error) {
      console.error('Cache error:', error);
      next();
    }
  };
};

// Usage examples
app.get('/api/users/profile', authenticateToken, cache(300), getUserProfile);
app.get('/api/payments/history', authenticateToken, cache(60), getPaymentHistory);
```

#### Application-Level Caching
```javascript
// In-memory cache for frequently accessed data
const NodeCache = require('node-cache');
const cache = new NodeCache({ 
  stdTTL: 600, // 10 minutes default TTL
  checkperiod: 120 // check for expired keys every 2 minutes
});

// Cache user data
const getCachedUser = async (userId) => {
  const cacheKey = `user:${userId}`;
  let user = cache.get(cacheKey);
  
  if (!user) {
    user = await User.findById(userId).lean();
    if (user) {
      cache.set(cacheKey, user, 300); // cache for 5 minutes
    }
  }
  
  return user;
};

// Cache payment statistics
const getCachedPaymentStats = async (userId) => {
  const cacheKey = `payment_stats:${userId}`;
  let stats = cache.get(cacheKey);
  
  if (!stats) {
    stats = await Payment.aggregate([
      { $match: { userId: new ObjectId(userId) } },
      {
        $group: {
          _id: null,
          totalAmount: { $sum: '$amount' },
          totalCount: { $sum: 1 },
          averageAmount: { $avg: '$amount' },
          lastPayment: { $max: '$createdAt' }
        }
      }
    ]);
    
    if (stats.length > 0) {
      cache.set(cacheKey, stats[0], 600); // cache for 10 minutes
    }
  }
  
  return stats;
};
```

### 5. Database Monitoring

#### Performance Monitoring
```javascript
// Database performance monitoring
const monitorDBPerformance = () => {
  setInterval(async () => {
    try {
      // PostgreSQL monitoring
      const pgStats = await pool.query(`
        SELECT 
          datname,
          numbackends,
          xact_commit,
          xact_rollback,
          blks_read,
          blks_hit,
          tup_returned,
          tup_fetched,
          tup_inserted,
          tup_updated,
          tup_deleted
        FROM pg_stat_database 
        WHERE datname = current_database()
      `);
      
      // MongoDB monitoring
      const mongoStats = await db.stats();
      
      // Log performance metrics
      console.log('Database Performance:', {
        timestamp: new Date(),
        postgres: pgStats.rows[0],
        mongodb: {
          collections: mongoStats.collections,
          dataSize: mongoStats.dataSize,
          indexSize: mongoStats.indexSize,
          objects: mongoStats.objects
        }
      });
      
      // Alert if performance is poor
      if (pgStats.rows[0].blks_hit / (pgStats.rows[0].blks_read + pgStats.rows[0].blks_hit) < 0.9) {
        console.warn('Low cache hit ratio detected');
      }
      
    } catch (error) {
      console.error('Database monitoring error:', error);
    }
  }, 60000); // check every minute
};

// Query performance monitoring
const monitorQueryPerformance = (query, params = []) => {
  const start = Date.now();
  
  return query.then(result => {
    const duration = Date.now() - start;
    
    if (duration > 1000) { // log slow queries
      console.warn('Slow query detected:', {
        duration,
        query: query.toString(),
        params
      });
    }
    
    return result;
  });
};
```

#### Automated Optimization
```javascript
// Automated index recommendation
const analyzeIndexes = async () => {
  const recommendations = [];
  
  // Analyze unused indexes
  const unusedIndexes = await db.query(`
    SELECT 
      schemaname,
      tablename,
      indexname,
      idx_scan,
      pg_size_pretty(pg_relation_size(indexrelid)) as size
    FROM pg_stat_user_indexes 
    WHERE idx_scan = 0
    ORDER BY pg_relation_size(indexrelid) DESC
  `);
  
  // Analyze missing indexes
  const missingIndexes = await db.query(`
    SELECT 
      query,
      calls,
      mean_time,
      rows
    FROM pg_stat_statements 
    WHERE mean_time > 100
    AND query NOT LIKE '%pg_stat_statements%'
    ORDER BY mean_time DESC
  `);
  
  // Generate recommendations
  if (unusedIndexes.rows.length > 0) {
    recommendations.push({
      type: 'remove_unused_indexes',
      indexes: unusedIndexes.rows,
      impact: 'Reduce storage and improve write performance'
    });
  }
  
  if (missingIndexes.rows.length > 0) {
    recommendations.push({
      type: 'add_missing_indexes',
      queries: missingIndexes.rows,
      impact: 'Improve query performance'
    });
  }
  
  return recommendations;
};
```

## Критерии приемки

### 1. Performance Metrics
- [ ] Query response time < 100ms for simple queries
- [ ] Query response time < 500ms for complex queries
- [ ] Database connection time < 50ms
- [ ] Cache hit ratio > 90%
- [ ] Memory usage optimized

### 2. Scalability
- [ ] Handles 1000+ concurrent connections
- [ ] Supports horizontal scaling
- [ ] Connection pooling optimized
- [ ] Load balancing ready
- [ ] Auto-scaling configured

### 3. Monitoring
- [ ] Performance metrics collected
- [ ] Slow query logging active
- [ ] Automated alerts configured
- [ ] Dashboard available
- [ ] Historical data stored

### 4. Optimization
- [ ] All slow queries optimized
- [ ] Indexes created and optimized
- [ ] Caching implemented
- [ ] Connection pooling configured
- [ ] Resource usage optimized

## Риски и митигация

### Риски
1. **Index overhead**
   - Митигация: Regular index analysis, unused index removal

2. **Cache invalidation**
   - Митигация: TTL configuration, cache invalidation strategies

3. **Connection pool exhaustion**
   - Митигация: Pool size monitoring, connection timeout configuration

## Deliverables

1. **Optimized Database**
   - Indexes created and optimized
   - Queries optimized
   - Connection pooling configured
   - Caching implemented

2. **Monitoring Setup**
   - Performance monitoring
   - Alerting configuration
   - Dashboard creation
   - Historical data collection

3. **Documentation**
   - Optimization guide
   - Performance tuning procedures
   - Monitoring setup guide
   - Troubleshooting guide

## Временные рамки

- **День 1:** Performance analysis, indexing strategy, query optimization
- **День 2:** Caching implementation, monitoring setup, testing
