# Техническое задание: Реализация пользовательского интерфейса для отображения данных о транзакциях

## Общая информация
- **Приоритет:** major
- **Оценка:** 5 SP
- **Исполнитель:** frontend
- **Срок выполнения:** 5 дней

## Цель
Создать компонент интерфейса, отображающий список транзакций с возможностью сортировки, фильтрации и пагинации, обеспечивающий удобный и интуитивный пользовательский опыт.

## Требования

### Функциональные требования
1. **Transaction Display:** Отображение списка транзакций с детальной информацией
2. **Sorting:** Сортировка по различным полям (дата, сумма, статус)
3. **Filtering:** Фильтрация по статусу, типу, дате
4. **Pagination:** Пагинация для больших объемов данных
5. **Search:** Поиск по транзакциям
6. **Export:** Экспорт данных в различных форматах

### Нефункциональные требования
1. **Performance:** Отображение 1000+ транзакций без задержек
2. **Responsive:** Адаптивный дизайн для всех устройств
3. **Accessibility:** WCAG 2.1 AA compliance
4. **UX:** Интуитивный и современный интерфейс
5. **Real-time:** Обновление данных в реальном времени

## Архитектура компонентов

### 1. Component Structure
```
src/
├── components/
│   ├── transactions/
│   │   ├── TransactionList.tsx
│   │   ├── TransactionItem.tsx
│   │   ├── TransactionFilters.tsx
│   │   ├── TransactionSearch.tsx
│   │   ├── TransactionPagination.tsx
│   │   ├── TransactionExport.tsx
│   │   └── index.ts
│   ├── ui/
│   │   ├── Table.tsx
│   │   ├── Select.tsx
│   │   ├── DatePicker.tsx
│   │   ├── Modal.tsx
│   │   └── index.ts
│   └── layout/
│       ├── DashboardLayout.tsx
│       └── Sidebar.tsx
├── hooks/
│   ├── useTransactions.ts
│   ├── useFilters.ts
│   ├── usePagination.ts
│   └── useExport.ts
├── services/
│   ├── transactionService.ts
│   └── exportService.ts
├── utils/
│   ├── formatters.ts
│   ├── validators.ts
│   └── constants.ts
└── types/
    ├── transaction.ts
    └── common.ts
```

### 2. Transaction List Component
```tsx
import React, { useState, useEffect, useMemo } from 'react';
import { useTransactions } from '../../hooks/useTransactions';
import { useFilters } from '../../hooks/useFilters';
import { usePagination } from '../../hooks/usePagination';
import { TransactionItem } from './TransactionItem';
import { TransactionFilters } from './TransactionFilters';
import { TransactionSearch } from './TransactionSearch';
import { TransactionPagination } from './TransactionPagination';
import { TransactionExport } from './TransactionExport';
import { Table } from '../ui/Table';
import { Button } from '../ui/Button';
import { Modal } from '../ui/Modal';

interface TransactionListProps {
  userId: string;
  onTransactionSelect?: (transaction: Transaction) => void;
}

export const TransactionList: React.FC<TransactionListProps> = ({
  userId,
  onTransactionSelect
}) => {
  const [selectedTransactions, setSelectedTransactions] = useState<string[]>([]);
  const [showFilters, setShowFilters] = useState(false);
  const [showExportModal, setShowExportModal] = useState(false);
  
  const {
    filters,
    updateFilter,
    clearFilters,
    hasActiveFilters
  } = useFilters();
  
  const {
    page,
    limit,
    totalPages,
    goToPage,
    nextPage,
    prevPage
  } = usePagination();
  
  const {
    transactions,
    isLoading,
    error,
    refetch,
    totalCount
  } = useTransactions({
    userId,
    filters,
    page,
    limit
  });

  const handleTransactionSelect = (transaction: Transaction) => {
    if (onTransactionSelect) {
      onTransactionSelect(transaction);
    }
  };

  const handleSelectAll = () => {
    if (selectedTransactions.length === transactions.length) {
      setSelectedTransactions([]);
    } else {
      setSelectedTransactions(transactions.map(t => t.id));
    }
  };

  const handleSelectTransaction = (transactionId: string) => {
    setSelectedTransactions(prev => 
      prev.includes(transactionId)
        ? prev.filter(id => id !== transactionId)
        : [...prev, transactionId]
    );
  };

  const handleExport = async (format: 'csv' | 'pdf' | 'excel') => {
    try {
      await exportTransactions(selectedTransactions, format);
      setShowExportModal(false);
      setSelectedTransactions([]);
    } catch (error) {
      console.error('Export failed:', error);
    }
  };

  if (error) {
    return (
      <div className="bg-red-50 border border-red-200 rounded-md p-4">
        <p className="text-sm text-red-600">
          Failed to load transactions. Please try again.
        </p>
        <Button onClick={refetch} variant="outline" className="mt-2">
          Retry
        </Button>
      </div>
    );
  }

  return (
    <div className="space-y-6">
      {/* Header */}
      <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between">
        <div>
          <h1 className="text-2xl font-bold text-gray-900">
            Transactions
          </h1>
          <p className="text-sm text-gray-600">
            {totalCount} transactions found
          </p>
        </div>
        
        <div className="mt-4 sm:mt-0 flex space-x-3">
          <Button
            variant="outline"
            onClick={() => setShowFilters(!showFilters)}
            className={hasActiveFilters ? 'border-blue-500 text-blue-600' : ''}
          >
            Filters {hasActiveFilters && `(${Object.keys(filters).length})`}
          </Button>
          
          {selectedTransactions.length > 0 && (
            <Button
              variant="outline"
              onClick={() => setShowExportModal(true)}
            >
              Export ({selectedTransactions.length})
            </Button>
          )}
          
          <Button
            variant="primary"
            onClick={refetch}
            loading={isLoading}
          >
            Refresh
          </Button>
        </div>
      </div>

      {/* Filters */}
      {showFilters && (
        <TransactionFilters
          filters={filters}
          onFilterChange={updateFilter}
          onClearFilters={clearFilters}
        />
      )}

      {/* Search */}
      <TransactionSearch
        onSearch={(query) => updateFilter('search', query)}
        placeholder="Search transactions..."
      />

      {/* Transaction Table */}
      <div className="bg-white shadow rounded-lg overflow-hidden">
        <Table>
          <Table.Header>
            <Table.Row>
              <Table.HeaderCell>
                <input
                  type="checkbox"
                  checked={selectedTransactions.length === transactions.length && transactions.length > 0}
                  onChange={handleSelectAll}
                  className="h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded"
                />
              </Table.HeaderCell>
              <Table.HeaderCell sortable onSort={(direction) => updateFilter('sort', 'date', direction)}>
                Date
              </Table.HeaderCell>
              <Table.HeaderCell sortable onSort={(direction) => updateFilter('sort', 'amount', direction)}>
                Amount
              </Table.HeaderCell>
              <Table.HeaderCell sortable onSort={(direction) => updateFilter('sort', 'status', direction)}>
                Status
              </Table.HeaderCell>
              <Table.HeaderCell sortable onSort={(direction) => updateFilter('sort', 'type', direction)}>
                Type
              </Table.HeaderCell>
              <Table.HeaderCell>
                Description
              </Table.HeaderCell>
              <Table.HeaderCell>
                Actions
              </Table.HeaderCell>
            </Table.Row>
          </Table.Header>
          
          <Table.Body>
            {isLoading ? (
              <Table.Row>
                <Table.Cell colSpan={7} className="text-center py-8">
                  <div className="flex items-center justify-center">
                    <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600"></div>
                    <span className="ml-2 text-gray-600">Loading transactions...</span>
                  </div>
                </Table.Cell>
              </Table.Row>
            ) : transactions.length === 0 ? (
              <Table.Row>
                <Table.Cell colSpan={7} className="text-center py-8">
                  <div className="text-gray-500">
                    <svg className="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                    </svg>
                    <h3 className="mt-2 text-sm font-medium text-gray-900">No transactions</h3>
                    <p className="mt-1 text-sm text-gray-500">
                      Get started by creating a new transaction.
                    </p>
                  </div>
                </Table.Cell>
              </Table.Row>
            ) : (
              transactions.map((transaction) => (
                <TransactionItem
                  key={transaction.id}
                  transaction={transaction}
                  isSelected={selectedTransactions.includes(transaction.id)}
                  onSelect={() => handleSelectTransaction(transaction.id)}
                  onClick={() => handleTransactionSelect(transaction)}
                />
              ))
            )}
          </Table.Body>
        </Table>
      </div>

      {/* Pagination */}
      {totalPages > 1 && (
        <TransactionPagination
          currentPage={page}
          totalPages={totalPages}
          onPageChange={goToPage}
          onNextPage={nextPage}
          onPrevPage={prevPage}
        />
      )}

      {/* Export Modal */}
      <Modal
        isOpen={showExportModal}
        onClose={() => setShowExportModal(false)}
        title="Export Transactions"
      >
        <TransactionExport
          selectedCount={selectedTransactions.length}
          onExport={handleExport}
          onCancel={() => setShowExportModal(false)}
        />
      </Modal>
    </div>
  );
};
```

### 3. Transaction Item Component
```tsx
import React from 'react';
import { Transaction } from '../../types/transaction';
import { formatCurrency, formatDate, getStatusColor, getStatusIcon } from '../../utils/formatters';
import { Table } from '../ui/Table';

interface TransactionItemProps {
  transaction: Transaction;
  isSelected: boolean;
  onSelect: () => void;
  onClick: () => void;
}

export const TransactionItem: React.FC<TransactionItemProps> = ({
  transaction,
  isSelected,
  onSelect,
  onClick
}) => {
  const statusColor = getStatusColor(transaction.status);
  const statusIcon = getStatusIcon(transaction.status);

  return (
    <Table.Row
      className={`cursor-pointer hover:bg-gray-50 ${isSelected ? 'bg-blue-50' : ''}`}
      onClick={onClick}
    >
      <Table.Cell>
        <input
          type="checkbox"
          checked={isSelected}
          onChange={onSelect}
          onClick={(e) => e.stopPropagation()}
          className="h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded"
        />
      </Table.Cell>
      
      <Table.Cell>
        <div className="text-sm text-gray-900">
          {formatDate(transaction.createdAt)}
        </div>
        <div className="text-xs text-gray-500">
          {transaction.id}
        </div>
      </Table.Cell>
      
      <Table.Cell>
        <div className={`text-sm font-medium ${
          transaction.amount > 0 ? 'text-green-600' : 'text-red-600'
        }`}>
          {transaction.amount > 0 ? '+' : ''}{formatCurrency(transaction.amount, transaction.currency)}
        </div>
        <div className="text-xs text-gray-500">
          {transaction.currency}
        </div>
      </Table.Cell>
      
      <Table.Cell>
        <span className={`inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium ${statusColor}`}>
          {statusIcon}
          <span className="ml-1">{transaction.status}</span>
        </span>
      </Table.Cell>
      
      <Table.Cell>
        <div className="text-sm text-gray-900">
          {transaction.type}
        </div>
        <div className="text-xs text-gray-500">
          {transaction.paymentMethod}
        </div>
      </Table.Cell>
      
      <Table.Cell>
        <div className="text-sm text-gray-900 truncate max-w-xs">
          {transaction.description}
        </div>
        {transaction.metadata?.reference && (
          <div className="text-xs text-gray-500">
            Ref: {transaction.metadata.reference}
          </div>
        )}
      </Table.Cell>
      
      <Table.Cell>
        <div className="flex items-center space-x-2">
          <button
            onClick={(e) => {
              e.stopPropagation();
              // Handle view details
            }}
            className="text-blue-600 hover:text-blue-900 text-sm"
          >
            View
          </button>
          {transaction.status === 'completed' && (
            <button
              onClick={(e) => {
                e.stopPropagation();
                // Handle refund
              }}
              className="text-red-600 hover:text-red-900 text-sm"
            >
              Refund
            </button>
          )}
        </div>
      </Table.Cell>
    </Table.Row>
  );
};
```

### 4. Transaction Filters Component
```tsx
import React from 'react';
import { Select } from '../ui/Select';
import { DatePicker } from '../ui/DatePicker';
import { Button } from '../ui/Button';

interface TransactionFiltersProps {
  filters: Record<string, any>;
  onFilterChange: (key: string, value: any) => void;
  onClearFilters: () => void;
}

export const TransactionFilters: React.FC<TransactionFiltersProps> = ({
  filters,
  onFilterChange,
  onClearFilters
}) => {
  const statusOptions = [
    { value: '', label: 'All Statuses' },
    { value: 'pending', label: 'Pending' },
    { value: 'processing', label: 'Processing' },
    { value: 'completed', label: 'Completed' },
    { value: 'failed', label: 'Failed' },
    { value: 'cancelled', label: 'Cancelled' },
    { value: 'refunded', label: 'Refunded' }
  ];

  const typeOptions = [
    { value: '', label: 'All Types' },
    { value: 'payment', label: 'Payment' },
    { value: 'refund', label: 'Refund' },
    { value: 'transfer', label: 'Transfer' },
    { value: 'withdrawal', label: 'Withdrawal' }
  ];

  const amountRangeOptions = [
    { value: '', label: 'All Amounts' },
    { value: '0-100', label: '$0 - $100' },
    { value: '100-500', label: '$100 - $500' },
    { value: '500-1000', label: '$500 - $1,000' },
    { value: '1000+', label: '$1,000+' }
  ];

  return (
    <div className="bg-white p-6 rounded-lg shadow border">
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
        <div>
          <label className="block text-sm font-medium text-gray-700 mb-2">
            Status
          </label>
          <Select
            value={filters.status || ''}
            onChange={(value) => onFilterChange('status', value)}
            options={statusOptions}
            placeholder="Select status"
          />
        </div>

        <div>
          <label className="block text-sm font-medium text-gray-700 mb-2">
            Type
          </label>
          <Select
            value={filters.type || ''}
            onChange={(value) => onFilterChange('type', value)}
            options={typeOptions}
            placeholder="Select type"
          />
        </div>

        <div>
          <label className="block text-sm font-medium text-gray-700 mb-2">
            Amount Range
          </label>
          <Select
            value={filters.amountRange || ''}
            onChange={(value) => onFilterChange('amountRange', value)}
            options={amountRangeOptions}
            placeholder="Select range"
          />
        </div>

        <div>
          <label className="block text-sm font-medium text-gray-700 mb-2">
            Date Range
          </label>
          <DatePicker
            value={filters.dateRange}
            onChange={(value) => onFilterChange('dateRange', value)}
            placeholder="Select date range"
            range
          />
        </div>
      </div>

      <div className="mt-4 flex justify-between">
        <Button
          variant="outline"
          onClick={onClearFilters}
        >
          Clear Filters
        </Button>
        
        <div className="text-sm text-gray-500">
          {Object.keys(filters).filter(key => filters[key]).length} filters active
        </div>
      </div>
    </div>
  );
};
```

### 5. Transaction Search Component
```tsx
import React, { useState, useEffect } from 'react';
import { Input } from '../ui/Input';

interface TransactionSearchProps {
  onSearch: (query: string) => void;
  placeholder?: string;
  debounceMs?: number;
}

export const TransactionSearch: React.FC<TransactionSearchProps> = ({
  onSearch,
  placeholder = "Search transactions...",
  debounceMs = 300
}) => {
  const [query, setQuery] = useState('');

  useEffect(() => {
    const timer = setTimeout(() => {
      onSearch(query);
    }, debounceMs);

    return () => clearTimeout(timer);
  }, [query, onSearch, debounceMs]);

  return (
    <div className="relative">
      <Input
        type="text"
        value={query}
        onChange={(e) => setQuery(e.target.value)}
        placeholder={placeholder}
        className="pl-10"
        rightIcon={
          query ? (
            <button
              onClick={() => setQuery('')}
              className="text-gray-400 hover:text-gray-600"
            >
              <svg className="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
              </svg>
            </button>
          ) : (
            <svg className="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
            </svg>
          )
        }
      />
    </div>
  );
};
```

### 6. Transaction Pagination Component
```tsx
import React from 'react';
import { Button } from '../ui/Button';

interface TransactionPaginationProps {
  currentPage: number;
  totalPages: number;
  onPageChange: (page: number) => void;
  onNextPage: () => void;
  onPrevPage: () => void;
}

export const TransactionPagination: React.FC<TransactionPaginationProps> = ({
  currentPage,
  totalPages,
  onPageChange,
  onNextPage,
  onPrevPage
}) => {
  const getPageNumbers = () => {
    const pages = [];
    const maxVisible = 5;
    
    if (totalPages <= maxVisible) {
      for (let i = 1; i <= totalPages; i++) {
        pages.push(i);
      }
    } else {
      const start = Math.max(1, currentPage - 2);
      const end = Math.min(totalPages, start + maxVisible - 1);
      
      if (start > 1) {
        pages.push(1);
        if (start > 2) {
          pages.push('...');
        }
      }
      
      for (let i = start; i <= end; i++) {
        pages.push(i);
      }
      
      if (end < totalPages) {
        if (end < totalPages - 1) {
          pages.push('...');
        }
        pages.push(totalPages);
      }
    }
    
    return pages;
  };

  return (
    <div className="flex items-center justify-between border-t border-gray-200 bg-white px-4 py-3 sm:px-6">
      <div className="flex flex-1 justify-between sm:hidden">
        <Button
          variant="outline"
          onClick={onPrevPage}
          disabled={currentPage === 1}
        >
          Previous
        </Button>
        <Button
          variant="outline"
          onClick={onNextPage}
          disabled={currentPage === totalPages}
        >
          Next
        </Button>
      </div>
      
      <div className="hidden sm:flex sm:flex-1 sm:items-center sm:justify-between">
        <div>
          <p className="text-sm text-gray-700">
            Showing page <span className="font-medium">{currentPage}</span> of{' '}
            <span className="font-medium">{totalPages}</span>
          </p>
        </div>
        
        <div>
          <nav className="isolate inline-flex -space-x-px rounded-md shadow-sm">
            <Button
              variant="outline"
              onClick={onPrevPage}
              disabled={currentPage === 1}
              className="rounded-l-md"
            >
              Previous
            </Button>
            
            {getPageNumbers().map((page, index) => (
              <React.Fragment key={index}>
                {page === '...' ? (
                  <span className="relative inline-flex items-center px-4 py-2 text-sm font-semibold text-gray-700 ring-1 ring-inset ring-gray-300 focus:outline-offset-0">
                    ...
                  </span>
                ) : (
                  <Button
                    variant={currentPage === page ? 'primary' : 'outline'}
                    onClick={() => onPageChange(page as number)}
                    className="rounded-none"
                  >
                    {page}
                  </Button>
                )}
              </React.Fragment>
            ))}
            
            <Button
              variant="outline"
              onClick={onNextPage}
              disabled={currentPage === totalPages}
              className="rounded-r-md"
            >
              Next
            </Button>
          </nav>
        </div>
      </div>
    </div>
  );
};
```

### 7. Transaction Export Component
```tsx
import React, { useState } from 'react';
import { Button } from '../ui/Button';
import { Select } from '../ui/Select';

interface TransactionExportProps {
  selectedCount: number;
  onExport: (format: 'csv' | 'pdf' | 'excel') => void;
  onCancel: () => void;
}

export const TransactionExport: React.FC<TransactionExportProps> = ({
  selectedCount,
  onExport,
  onCancel
}) => {
  const [format, setFormat] = useState<'csv' | 'pdf' | 'excel'>('csv');
  const [isExporting, setIsExporting] = useState(false);

  const formatOptions = [
    { value: 'csv', label: 'CSV (Excel compatible)' },
    { value: 'excel', label: 'Excel (.xlsx)' },
    { value: 'pdf', label: 'PDF Document' }
  ];

  const handleExport = async () => {
    setIsExporting(true);
    try {
      await onExport(format);
    } finally {
      setIsExporting(false);
    }
  };

  return (
    <div className="space-y-4">
      <div>
        <p className="text-sm text-gray-600">
          Export {selectedCount} selected transaction{selectedCount !== 1 ? 's' : ''}
        </p>
      </div>

      <div>
        <label className="block text-sm font-medium text-gray-700 mb-2">
          Export Format
        </label>
        <Select
          value={format}
          onChange={(value) => setFormat(value as 'csv' | 'pdf' | 'excel')}
          options={formatOptions}
        />
      </div>

      <div className="bg-blue-50 border border-blue-200 rounded-md p-4">
        <div className="flex">
          <div className="flex-shrink-0">
            <svg className="h-5 w-5 text-blue-400" fill="currentColor" viewBox="0 0 20 20">
              <path fillRule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clipRule="evenodd" />
            </svg>
          </div>
          <div className="ml-3">
            <h3 className="text-sm font-medium text-blue-800">
              Export Information
            </h3>
            <div className="mt-2 text-sm text-blue-700">
              <ul className="list-disc pl-5 space-y-1">
                <li>CSV format includes all transaction details</li>
                <li>PDF format includes formatted report with charts</li>
                <li>Excel format includes multiple sheets with analysis</li>
              </ul>
            </div>
          </div>
        </div>
      </div>

      <div className="flex justify-end space-x-3">
        <Button
          variant="outline"
          onClick={onCancel}
          disabled={isExporting}
        >
          Cancel
        </Button>
        <Button
          variant="primary"
          onClick={handleExport}
          loading={isExporting}
        >
          Export {format.toUpperCase()}
        </Button>
      </div>
    </div>
  );
};
```

### 8. Custom Hooks

#### useTransactions Hook
```tsx
import { useState, useEffect, useCallback } from 'react';
import { transactionService } from '../services/transactionService';
import { Transaction } from '../types/transaction';

interface UseTransactionsOptions {
  userId: string;
  filters: Record<string, any>;
  page: number;
  limit: number;
}

export const useTransactions = ({
  userId,
  filters,
  page,
  limit
}: UseTransactionsOptions) => {
  const [transactions, setTransactions] = useState<Transaction[]>([]);
  const [isLoading, setIsLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [totalCount, setTotalCount] = useState(0);

  const fetchTransactions = useCallback(async () => {
    setIsLoading(true);
    setError(null);
    
    try {
      const response = await transactionService.getTransactions({
        userId,
        ...filters,
        page,
        limit
      });
      
      setTransactions(response.data);
      setTotalCount(response.total);
    } catch (err: any) {
      setError(err.message || 'Failed to fetch transactions');
    } finally {
      setIsLoading(false);
    }
  }, [userId, filters, page, limit]);

  useEffect(() => {
    fetchTransactions();
  }, [fetchTransactions]);

  const refetch = useCallback(() => {
    fetchTransactions();
  }, [fetchTransactions]);

  return {
    transactions,
    isLoading,
    error,
    totalCount,
    refetch
  };
};
```

#### useFilters Hook
```tsx
import { useState, useCallback } from 'react';

export const useFilters = () => {
  const [filters, setFilters] = useState<Record<string, any>>({});

  const updateFilter = useCallback((key: string, value: any) => {
    setFilters(prev => ({
      ...prev,
      [key]: value
    }));
  }, []);

  const clearFilters = useCallback(() => {
    setFilters({});
  }, []);

  const hasActiveFilters = Object.values(filters).some(value => 
    value !== null && value !== undefined && value !== ''
  );

  return {
    filters,
    updateFilter,
    clearFilters,
    hasActiveFilters
  };
};
```

### 9. Utility Functions
```tsx
// formatters.ts
export const formatCurrency = (amount: number, currency: string = 'USD'): string => {
  return new Intl.NumberFormat('en-US', {
    style: 'currency',
    currency: currency
  }).format(amount);
};

export const formatDate = (date: string | Date): string => {
  return new Intl.DateTimeFormat('en-US', {
    year: 'numeric',
    month: 'short',
    day: 'numeric',
    hour: '2-digit',
    minute: '2-digit'
  }).format(new Date(date));
};

export const getStatusColor = (status: string): string => {
  const colors = {
    pending: 'bg-yellow-100 text-yellow-800',
    processing: 'bg-blue-100 text-blue-800',
    completed: 'bg-green-100 text-green-800',
    failed: 'bg-red-100 text-red-800',
    cancelled: 'bg-gray-100 text-gray-800',
    refunded: 'bg-purple-100 text-purple-800'
  };
  
  return colors[status as keyof typeof colors] || 'bg-gray-100 text-gray-800';
};

export const getStatusIcon = (status: string): React.ReactNode => {
  const icons = {
    pending: '⏳',
    processing: '🔄',
    completed: '✅',
    failed: '❌',
    cancelled: '🚫',
    refunded: '↩️'
  };
  
  return icons[status as keyof typeof icons] || '❓';
};
```

## Критерии приемки

### 1. Functionality
- [ ] Transaction list отображается корректно
- [ ] Sorting работает по всем полям
- [ ] Filtering функционирует
- [ ] Search работает
- [ ] Pagination активна
- [ ] Export работает

### 2. Performance
- [ ] 1000+ транзакций отображаются без задержек
- [ ] Virtual scrolling работает
- [ ] Lazy loading активен
- [ ] Debounced search работает
- [ ] Memory usage оптимизирован

### 3. UX/UI
- [ ] Responsive design работает
- [ ] Loading states отображаются
- [ ] Error states обрабатываются
- [ ] Accessibility standards соблюдены
- [ ] Animations плавные

### 4. Testing
- [ ] Unit tests покрывают 90%+ кода
- [ ] Integration tests проходят
- [ ] E2E tests работают
- [ ] Performance tests пройдены
- [ ] Accessibility tests пройдены

## Риски и митигация

### Риски
1. **Performance issues с большими данными**
   - Митигация: Virtual scrolling, pagination, lazy loading

2. **Memory leaks**
   - Митигация: Proper cleanup, useEffect dependencies

3. **Complex state management**
   - Митигация: Custom hooks, state normalization

## Deliverables

1. **Transaction Components**
   - TransactionList
   - TransactionItem
   - TransactionFilters
   - TransactionSearch
   - TransactionPagination
   - TransactionExport

2. **Custom Hooks**
   - useTransactions
   - useFilters
   - usePagination
   - useExport

3. **Testing Suite**
   - Unit tests
   - Integration tests
   - E2E tests
   - Performance tests

4. **Documentation**
   - Component documentation
   - API documentation
   - User guide
   - Developer guide

## Временные рамки

- **День 1:** Component structure, basic UI
- **День 2:** Transaction list, item components
- **День 3:** Filters, search, pagination
- **День 4:** Export functionality, performance optimization
- **День 5:** Testing, documentation, final polish
